# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/

# Python
__pycache__/
*.pyc

# harness-generated artifacts have their own tests/.gitignore
# check_svg2pptx.py fixtures when the gate is run from the repo root
fix_*.pptx
fix_slide*.svg
fixlib/
fixlib2/

# local sweep machine config (secrets referenced via env vars, but the
# tenant/client ids and machine paths don't belong in the repo either)
config.json
config.hosted.json

# local sweep workDir when placed inside the repo (run logs + cached
# sign-in tokens - must never be committed)
work/

# a local export of the sidecar library for analysis (never committed;
# the 2026-09-05 snapshot is in git history under LRSDocIndex/)
corpus/
